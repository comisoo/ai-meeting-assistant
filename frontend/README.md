# Deprecated Frontend

This directory is no longer the active frontend codebase.

The maintained frontend now lives in:

- `frontend-react/`

Run the active frontend with:

```bash
cd frontend-react
npm run dev
```

This folder is intentionally kept only to make the migration explicit and to prevent accidental work on the retired frontend implementation.
